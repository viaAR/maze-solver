# maze-solver
This program uses the stack and pair data structures to navigate through a maze. The logic attempts to solve the maze with a depth first search approach and will output its solution in a folder named 'solved'.
